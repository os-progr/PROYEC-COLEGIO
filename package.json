{
  "name": "colegio-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mobile-app"
  ],
  "scripts": {
    "build": "npm run build --workspace=backend"
  }
}
